#!/usr/bin/env bash

set -e

DIR=$(cd `dirname $0` && pwd)

touch "${DIR}/data.json"

docker image build \
  --no-cache \
  -t jtreminio/hellosign-python-sdk:latest \
  -f data_python/Dockerfile \
  ./data_python
